// Canvas rendering implementation lives in `src/components/editor/CanvasStage.tsx`.
// This module is reserved for canvas-specific helpers (coordinate transforms,
// hit testing, snapping, etc.) added in later phases per brief §17.
export {}
